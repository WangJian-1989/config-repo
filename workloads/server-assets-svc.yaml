apiVersion: v1
kind: Service
metadata:
  name: server-assets-service
  namespace: gitops-cicd
  labels:
    app: server-assets
spec:
  type: ClusterIP
  ports:
    - port: 9002
      targetPort: 9002
      protocol: TCP
    - port: 9502
      targetPort: 9502
      protocol: TCP
  selector:
    app: server-assets
